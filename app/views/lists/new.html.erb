<div class="container">
<h1>Create a list</h1>
</div>
<div class="container">
<%= simple_form_for(@list) do |f| %>
<%= f.input :name %>
<%= f.button :submit, class: 'btn btn-success' %>

<% end %>
<p>
  <%= link_to "Back to lists", lists_path %>
</p>
</div>
